{% extends "base.html.twig" %}

{% block afterMapInit %}
        {% for entite in entites %}
            {{ createMarker(entite) }}
        {% endfor %}
    {% if center %}
        map.setCenter(new google.maps.LatLng({{ center }}));
    {% endif %}
{% endblock %}
