{% extends "base_blanche.html.twig" %}

{% block page_content %}
    <div class="row">
        <div class="col-xs-4 col-xs-offset-4">
            <img class="Photo" src="{{ cuisson.photoPath }}" alt="..." />
        </div>
        <div class="col-xs-12">
            <h1>
                {{ cuisson.nom }}
            </h1>
            <h3>
                Quelques conseils et astuces :
            </h3>
            <p>{{ cuisson.getConseilsAstuces(morceau.typeViande)|raw|nl2br }}</p>
            <p>
                {{ cuisson.description|raw|nl2br }}
            </p>
        </div>
    </div>
    <div class="row" style="padding-top:50px;">
        <div class="col-xs-12">
            <h3>Les morceaux à {{ cuisson.nom }}</h3>
        </div>
    </div>


    {% for morceauRec in morceauxRecommandes %}
        <div class="row">
            <div class="row ListeStyle2">
                <div class="col-xs-12">
                    <a class="link" href="{{ path_for("flash.morceau",{"categ":morceauRec.typeViande,"idMorceau":morceauRec.id}) }}" data-color="#ffffff">
                        <div class="Tof2"><img src="{{ morceauRec.photoPath }}" alt="..." /></div>
                        <div class="Description" style="padding:40px 20px">
                            <div class="Nom col-xs-12">{{ morceauRec.nom }}</div>
                        </div>
                    </a>
                </div>
            </div>
        </div>
    {% endfor %}
{% endblock %}
